# Optional overrides for local docker-compose services.
# The compose file already has the same defaults, so copying this file is optional.

POSTGRES_DB=jobs
POSTGRES_USER=jobs
POSTGRES_PASSWORD=jobs
MEILI_MASTER_KEY=dev_master_key_change_me
REDIS_PASSWORD=
