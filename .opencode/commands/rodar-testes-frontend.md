---
description: Run frontend lint and build validation for the Next.js app.
agent: tester
subtask: true
---
In `src/frontend`, run `npm run lint` and `npm run build`.

Focus area: `$ARGUMENTS`

Report:
1. command(s) run
2. pass/fail result
3. failing step or build issue
4. remaining validation gaps
