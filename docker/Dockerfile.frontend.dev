FROM node:22-alpine

WORKDIR /app

# Copy package files before mounting the volume so npm install runs during build,
# not at container startup. The node_modules layer is preserved via a named volume.
COPY src/frontend/package.json src/frontend/package-lock.json ./

RUN npm ci

EXPOSE 3000

CMD ["npm", "run", "dev"]
